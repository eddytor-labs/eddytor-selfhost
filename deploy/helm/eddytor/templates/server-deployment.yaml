apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eddytor.server.name" . }}
  labels:
    {{- include "eddytor.labels" . | nindent 4 }}
    app: {{ include "eddytor.server.name" . }}
spec:
  {{- if not .Values.server.autoscaling.enabled }}
  replicas: {{ .Values.server.replicas }}
  {{- end }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: {{ include "eddytor.server.name" . }}
  template:
    metadata:
      labels:
        {{- include "eddytor.labels" . | nindent 8 }}
        app: {{ include "eddytor.server.name" . }}
    spec:
      serviceAccountName: {{ include "eddytor.server.name" . }}
      terminationGracePeriodSeconds: 60
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- $waitInit := include "eddytor.waitForDbInit" . }}
      {{- if trim $waitInit }}
      initContainers:
        {{- $waitInit | nindent 8 }}
      {{- end }}
      containers:
        - name: eddytor-server
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}/eddytor-ce-server:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8082
              name: grpc-flight
          securityContext:
            {{- include "eddytor.securityContext" . | nindent 12 }}
          env:
            {{- include "eddytor.configEnv" . | nindent 12 }}
            {{- include "eddytor.secretEnv" . | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            {{- toYaml .Values.server.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              path: /healthz
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
      volumes:
        - name: tmp
          emptyDir: {}
